<view class="page">
  <view class="hero">
    <view class="hero-title">家长设置</view>
    <view class="hero-subtitle">管理 PIN、模板和隐私说明，让孩子的记录更安心。</view>
  </view>

  <view class="section-title">家长 PIN</view>
  <view class="card form">
    <input class="field" password maxlength="4" type="number" placeholder="当前 PIN，首次可留空" value="{{oldPin}}" bindinput="onOldPin" />
    <input class="field" password maxlength="4" type="number" placeholder="新的 4 位 PIN" value="{{newPin}}" bindinput="onNewPin" />
    <button class="primary-button" bindtap="savePin">保存 PIN</button>
  </view>

  <view class="section-title">快捷管理</view>
  <view class="grid-two">
    <button class="secondary-button" bindtap="goTasks">任务管理</button>
    <button class="secondary-button" bindtap="goTemplates">模板预览</button>
  </view>

  <view class="section-title">隐私与安全</view>
  <view class="card privacy">
    <view>只保存昵称、年龄、性别、任务记录和金币数据。</view>
    <view>不收集真实姓名、手机号、身份证、学校或精确位置。</view>
    <view>家长 PIN 仅保存哈希，不保存明文。</view>
  </view>
</view>
